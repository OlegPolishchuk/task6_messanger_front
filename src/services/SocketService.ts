import io from 'socket.io-client';
import {SendMessage} from "services/types";

// const SOCKET_PATH = 'http://localhost:5000/';
//
// const socket = io(SOCKET_PATH);
//
// export const socketService = {
//
//   connect: () => {
//     socket.connect();
//   },
//
//   sendMessage: (messageData: SendMessage) => {
//     socket.emit('message', messageData)
//   }
//
// }
export {}